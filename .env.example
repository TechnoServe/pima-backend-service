# Postgres connection string 
DATABASE_URL=postgresql://admin:pima2023@127.0.0.1:5432/pima
DB_SCHEMA=pima

# JWT secret 
JWT_SECRET=76760768706807687687687

# CORS (use '*' in dev, or comma-separated origins in prod)
CORS_ORIGINS=*
GCP_PROJECT_ID=pima-gcp
GCS_BUCKET_NAME=bucket-name-placeholder

GCS_PREFIX=prefix-placeholder

GCS_SIGNED_URL_TTL_SECONDS=3600