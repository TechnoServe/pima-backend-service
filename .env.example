# Postgres connection string (will be converted to asyncpg automatically if needed)
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/pima
DB_SCHEMA=pima

# JWT secret (change in prod)
JWT_SECRET=change-me

# CORS (use '*' in dev, or comma-separated origins in prod)
CORS_ORIGINS=*
